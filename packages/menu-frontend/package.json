{
  "name": "@ciska/menu-frontend",
  "version": "1.0.0",
  "main": "lib/cjs/index.js",
  "module": "lib/esm/index.js",
  "author": "tett23",
  "license": "MIT",
  "private": true,
  "scripts": {
    "clean": "rimraf lib",
    "build": "node build.mjs",
    "build:dependencies": "run-p build:dependencies:*",
    "build:dependencies:message": "yarn workspace @ciska/message build",
    "watch": "node build.mjs --watch",
    "watch:dependencies": "run-p watch:dependencies:*",
    "watch:dependencies:message": "yarn workspace @ciska/message watch",
    "lint": "run-p lint:ts",
    "lint:ts": "tsc --noEmit",
    "test": "NODE_OPTIONS=--experimental-vm-modules jest --passWithNoTests",
    "ci": "lint"
  },
  "dependencies": {
    "@ciska/message": "1.0.0",
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  },
  "devDependencies": {
    "@types/react-dom": "^17.0.14"
  }
}
