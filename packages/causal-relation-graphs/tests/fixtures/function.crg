
// 右結合
// let f :: Effect = x { x };
let f :: Int -> Int -> Int = x y { x + y } | 1 y { y };

// 高階関数
// f {Int -> Int -> Int = x y { x + y }};

// ラムダをパターンマッチで選んで実装
// {|x| x} | {|+1| +1};
// 後ろが実行される
// ({|x| x} | {|+1| +1})(+1)

// 呼び出し
// { |v| v }(+1);

// 呼びだし
// f +1

// カリー化について
// キャプチャについて

f
